Omnix AI - Powered By Gemini

Note:
    Create a Virtual Environment First

How to Use:
1.Create a Gemini api key from google ai studio.
2.Replace "os.getenv("API_KEY")" with your api key inside ""
3.In the terminal run "pip install ."
    -It will download all dependencies
4.It will all setup now

To run it:

In the terminal write "Streamlit run main.py"
it will ask for email - Enter your email
Now in the browser it will open